# 知识库建设关键节点日志（开发手册）

> **定位**：记录知识库（RAG）模块从 2026-08-27 搭建至今的全部关键节点，供后续开发与面试复盘使用。
> **口径声明**：所有性能数字均为实测值（标注实测日期）；「全命中」指当前 52 条评测集的评测结果，不代表系统能力上限；未落地项一律标注「未落地 / 设计」。
> **证据约定**：每个节点均附代码/文档路径，可按路径直接翻代码核对。git 首提交为 2026-08-31（`cff7eaa`，历史压缩快照），此前的节点时间以 `docs/项目文档.md` §7 轮次记录与工作日志为准。

---

## 一、当前状态一页速览

**入库链路**：`parse_file`（txt/md/csv/pdf/docx）→ `sanitize`（清洗+PII 脱敏）→ `chunk_text`（句界切分 500/128）→ `embed` → `vector_store.add`（doc_id + tenant_id 元数据）→ SQLite 登记

**检索链路**：查询 → 向量召回（top_k×3 扩容，上限 50）＋ BM25 召回（租户+语料指纹缓存）→ 租户过滤 → RRF 融合（k=60，权重 向量 0.4 / BM25 0.6）→ 可选重排序 → 返回（含来源溯源）

**当前实测基准（2026-09-17，`python scripts/eval_rag.py --topk 3`）**：
- 评测集 52 条，**Recall@3 = 52/52 全命中**，Recall@5 同为全命中
- **MRR@3 = 0.942**
- 平均检索耗时 **< 1 ms**（memory 向量库 + 缓存全热）

---

## 二、关键节点日志（时间线）

### 节点 0 ｜ 2026-08-27 ｜ 选型定调：统一语料 + Mock 优先可插拔

- **决策**：知识库统一英语语料，查询时先翻译再检索；LLM / Embedding / 向量库三组抽象全部走「接口 + 工厂 + Mock 实现」，无 API Key 也能跑通全链路。
- ** why**：跨境电商场景多语种查询是常态；Mock 优先保证脚手架离线可演示、后换真实模型零改代码。
- **证据**：`backend/app/core/embeddings/factory.py`、`backend/app/core/vector_store/factory.py`（`lru_cache` 单例 + 缺依赖降级 Mock）；`docs/项目文档.md`。

### 节点 1 ｜ 2026-08-27 ｜ v0.1：入库链路首次打通

- **做了什么**：`parse_file` 支持 txt/md/csv（utf-8 解码）、pdf（pypdf）、docx（python-docx），不支持类型显式报错；`sanitize` 清洗 + PII 脱敏；`chunk_text` 按段落→句子→硬切三级切分，默认 500 字符 / 重叠 128。
- **验证**：冒烟测试 6 项通过；导入 3 篇示例文档切出 4 个分块；检索正确命中。
- **证据**：`backend/app/services/knowledge.py`（`parse_file`/`add_document`）、`backend/app/utils/chunking.py`、`backend/app/utils/text_clean.py`（`sanitize`/`mask_pii`）、`backend/scripts/ingest.py`（批量导入 `data/knowledge/`）、`backend/app/config.py`（`CHUNK_SIZE=500`、`CHUNK_OVERLAP=128`）。

### 节点 2 ｜ 2026-08-27 ｜ 踩坑修复：memory 向量库跨进程丢数据

- **现象**：memory 向量库纯内存实现，进程重启后已入库文档全部丢失，检索为空。
- **修复**：`add`/`delete` 后自动 JSON 持久化到 `data/index/vector_store.json`，构造时 `_load()` 回载。
- **影响深远**：这个修复后来让 `eval_rag.py` 可以单进程直跑（见节点 12 的澄清）。
- **证据**：`backend/app/core/vector_store/memory.py`（`_save`/`_load`）、`backend/app/core/vector_store/factory.py`（`MEMORY_INDEX_PATH`）。

### 节点 3 ｜ 2026-08-28 ｜ 第一轮：BM25 索引缓存 + 写路径失效

- **做了什么**：BM25 索引按「(租户, 语料指纹)」缓存（指纹 = 文档 id 元组），避免每次检索全量重建；知识库增删后由 knowledge 服务调用 `invalidate_bm25_cache()` 强制失效。防御性设计：缓存只保留最新一份，避免并发增删导致堆积。
- **证据**：`backend/app/services/rag/retriever.py`（`_bm25_cache`、`_get_bm25`、`invalidate_bm25_cache`）、`backend/app/services/knowledge.py`（增删末尾调用失效）、`docs/项目文档.md` §7 第一轮。

### 节点 4 ｜ 2026-08-29 ｜ 第二轮：向量库归一化矩阵惰性缓存

- **做了什么**：memory 向量库把逐条余弦相似度改为「行归一化矩阵 × 查询向量」一次矩阵运算；矩阵惰性构建（`_norm_matrix`），add/delete/加载后由 `_invalidate_matrix()` 失效。
- **效果**：检索从 O(n) Python 循环变为 numpy 向量化，是后续「<1ms 检索」的基础。
- **证据**：`backend/app/core/vector_store/memory.py`（`_get_norm_matrix`、`search`）。

### 节点 5 ｜ 2026-08-29 ｜ 第三轮：评测基建（先有尺子再调优）

- **做了什么**：新增 `scripts/eval_rag.py`，对评测集逐条检索，计算 **Recall@K + MRR + 平均耗时**，并输出逐条命中排名与未命中样例；初始评测集 20 条（`data/eval/rag_eval.json`）。命中判定口径：`is_hit` = 结果文本包含评测案例的全部 `must` 关键词。
- ** why**：调权重/换模型前先固化评测口径，避免「凭感觉调优」。
- **证据**：`backend/scripts/eval_rag.py`、`backend/data/eval/rag_eval.json`、`docs/项目文档.md` §7 第三轮。

### 节点 6 ｜ 2026-08-29 ｜ 第三轮：重排序双实现（默认零依赖，可选真模型）

- **做了什么**：`rerank` 支持 `heuristic`（关键词覆盖率 + 检索得分各占 0.5，默认）与 `cross_encoder`（sentence-transformers CrossEncoder，如 bge-reranker）两种实现；cross_encoder 惰性加载，**依赖缺失自动回退 heuristic**；内部打分字段 `_rerank_score` 返回前移除，不泄漏到 API。
- **证据**：`backend/app/services/rag/rerank.py`、`backend/app/config.py`（`RERANK_PROVIDER="heuristic"`、`ENABLE_RERANK=False` 默认关闭）。

### 节点 7 ｜ 2026-08-29 ｜ 第三轮：Alembic 迁移体系建立

- **做了什么**：引入 Alembic 管理表结构，首个迁移 `1c0c2afea11a`（initial schema），替代手工建表；后续所有表变更走迁移文件。
- **证据**：`backend/migrations/versions/1c0c2afea11a_initial_schema.py`、`backend/migrations/versions/1ab18806c2f9_add_tenant_id_to_knowledge_docs_and_.py`。

### 节点 8 ｜ 2026-08-29 ｜ 第四轮：多租户隔离落地（知识库最关键的架构节点）

- **做了什么**：
  1. 迁移 `1ab18806c2f9` 给 `knowledge_docs`、`conversations` 加 `tenant_id` 列（server_default='default'，带索引）；
  2. 入库时向量元数据写入 `doc_id` + `tenant_id` + `chunk_index`；
  3. 检索两侧候选（向量召回 + BM25 全量列表）先过 `_tenant_filter`，**历史数据无 tenant_id 标记视为 default 租户**；
  4. 删除语义精确化：优先按 `doc_id` 匹配删向量，无 doc_id 的历史记录回退按 filename（仅在同名替换上下文中安全）；
  5. 同名替换：同一租户内重复上传同名文档 = 先删旧向量与旧记录再入库，避免知识重复膨胀。
- **证据**：`backend/migrations/versions/1ab18806c2f9_*.py`、`backend/app/services/rag/retriever.py`（`_tenant_filter`）、`backend/app/services/knowledge.py`（`add_document` 同名替换、`_delete_vectors_for`、`delete_document`）。

### 节点 9 ｜ 2026-08-29 ｜ 第四轮：评测集 20 → 52 条 + 混合检索权重调优

- **做了什么**：评测集扩到 52 条（覆盖贸易术语、物流、支付、售后等多主题）；混合权重由默认形参值调优为 **向量 0.4 / BM25 0.6（以 BM25 关键词为主）**——最终生效的是 `config.py` 的 `HYBRID_WEIGHT_VECTOR=0.4`/`HYBRID_WEIGHT_BM25=0.6`（`get_retriever` 用 settings 覆盖构造函数形参）。
- **结果**：调优后当前 52 条评测集**全命中（Recall@3=100%、MRR=0.942，2026-09-17 `eval_rag.py` 实测）**。历史过程对比数字（96.2%→100% 等）仅存于 `docs/项目文档.md` §7 作过程记录；**对外表述统一用「当前评测集全命中」，不写系统能力 100%**。
- **证据**：`backend/data/eval/rag_eval.json`（52 cases）、`backend/app/config.py`、`backend/app/services/rag/retriever.py`（`get_retriever`）、`docs/项目文档.md` §7 第四轮。

### 节点 10 ｜ 2026-08-30 ｜ 第六轮：LLM 故障转移（问答链路稳定性）

- **做了什么**：`LLM_FAILOVER` 端点链，连接失败/超时/429/5xx/401/403 自动切换备用端点（400 类业务错误不切换）；接入小米 MiMo 主端点。知识库问答的生成环节从此有多端点兜底。
- **证据**：`backend/app/core/llm/`（openai_compatible 实现内的 failover 逻辑）、`docs/项目文档.md` §7 第六轮。

### 节点 11 ｜ 2026-08-31 ｜ git 首提交快照

- 提交 `cff7eaa`（109 文件 / 8917 行），知识库链路（入库/检索/评测/迁移）全部纳入版本管理。
- **证据**：`git log`（`cff7eaa 08-31 init`）。

### 节点 12 ｜ 2026-09-07 ｜ 真实 LLM 底座切换：小米 MiMo V2.5

- **做了什么**：接入小米 MiMo V2.5 作为知识库问答的主 LLM（思考流式推送 + 故障转移），知识库回答从「演示级」进入「真实模型」阶段。
- **证据**：git `21781cc`（`feat: 接入小米 MiMo V2.5 大模型（思考流式推送+故障转移）`）。

### 节点 13 ｜ 2026-09-17 ｜ 口径校准与全量实测（防打假基准日）

- **做了什么**：
  1. **实测复核**：`python scripts/eval_rag.py --topk 3` 直跑 → 52/52 全命中、MRR 0.942、耗时 <1ms。同时澄清一个认知误区：评测脚本**无需同进程入库**——memory 向量库有 JSON 持久化，独立进程直跑即有数据（节点 2 修复的红利）。
  2. **修正文档错误**：混合权重此前在面试文档中写反——正确口径是**配置默认向量 0.4 / BM25 0.6**；构造函数里的 0.6/0.4 只是形参默认值，运行时被配置覆盖。
  3. **测试实测**：25 项测试 = 23 过 2 挂（挂的是 smoke 的 copywriting/chat，根因 MiMo 账户 402 欠费，非代码问题）；知识库相关单元测试全过。
- **证据**：`backend/scripts/eval_rag.py` 实测输出、`docs/简历面试/面试对话手册.md`（📌 实测参考值块）、`.workbuddy/memory/2026-09-17.md`。

### 节点 14 ｜ 2026-09-18 ｜ 文档体系重组 + 本手册诞生

- **做了什么**：README 重写为产品线结构，知识库以「📚 知识资产」产品线呈现；文档体系按「权威文档（项目文档）/ 简历面试 / 开发手册」分层；本文件即开发手册首篇。
- **证据**：git `8b0edcd`、`README.md`。

---

## 三、踩坑与修复对照表

| # | 坑 | 现象 | 修复 | 预防约定 |
|---|---|---|---|---|
| 1 | memory 向量库不持久化 | 进程重启后知识全丢，检索为空 | add/delete 后自动 JSON 持久化（`vector_store.json`） | 任何存储实现必须回答「重启后数据在哪」 |
| 2 | BM25 每次检索全量重建 | 大语料下检索变慢 | 按（租户, 语料指纹）缓存 + 写路径失效 | 缓存必须有明确失效时机，不能只靠 TTL |
| 3 | 混合权重文档写反 | 面试文档写「向量 0.6/BM25 0.4」，与运行时相反 | 统一为配置口径：向量 0.4 / BM25 0.6 | 形参默认值 ≠ 生效值，写文档以 config 为准 |
| 4 | 以为评测需同进程入库 | 不敢单独跑评测脚本 | 实测澄清：JSON 持久化使独立脚本可直跑 | 结论先实测再下，不凭直觉推断 |
| 5 | 历史向量无 tenant_id | 升级后旧数据检索被过滤光 | `_tenant_filter` 把无标记数据视为 default 租户 | 加列必须配 server_default + 兼容读 |
| 6 | 同名文档重复上传导致知识膨胀 | 同一篇文档占双份向量 | 同名替换语义：先删旧（按 doc_id 精确匹配）再入库 | 删除必须精确到 doc_id，filename 只作历史回退 |
| 7 | rerank 内部打分泄漏 | `_rerank_score` 混进 API 响应 | 返回前统一 `pop` | 中间计算字段不外泄，出接口前清理 |

---

## 四、当前设计要点速查表（改代码前先看这张表）

| 参数/机制 | 当前值 | 位置 |
|---|---|---|
| 分块大小 / 重叠 | 500 / 128 字符，句界优先 | `config.py`、`utils/chunking.py` |
| 检索 top_k | 5（召回扩容 ×3，上限 50） | `config.py RAG_TOP_K`、`rag/retriever.py` |
| RRF 融合 | k=60 | `rag/retriever.py _RRF_K` |
| 混合权重 | 向量 0.4 / BM25 0.6（配置生效值） | `config.py HYBRID_WEIGHT_*` |
| 重排序 | heuristic 默认；cross_encoder 可选+失败回退；默认关闭 | `config.py RERANK_PROVIDER`、`ENABLE_RERANK` |
| BM25 参数 | k1=1.5, b=0.75，纯 Python 实现 | `rag/bm25.py` |
| 向量库 | memory（JSON 持久化）/ chroma 可切换 | `core/vector_store/` |
| 租户隔离 | 元数据 tenant_id + 检索后过滤 + doc_id 精确删除 | `rag/retriever.py`、`services/knowledge.py` |
| 支持格式 | txt / md / csv / pdf / docx | `services/knowledge.py SUPPORTED_EXT` |

---

## 五、如何复现与验证

```bash
cd backend
# 1. 配置自检（当前 LLM/Embedding/向量库生效状态）
.venv/Scripts/python.exe scripts/check_config.py

# 2. 导入 data/knowledge/ 下全部文档
.venv/Scripts/python.exe scripts/ingest.py

# 3. 检索质量评测（当前基准：52 条全命中、MRR 0.942）
.venv/Scripts/python.exe scripts/eval_rag.py --topk 3

# 4. 知识库相关测试
.venv/Scripts/python.exe -m pytest tests -k "knowledge or tenant" -v
```

---

## 六、下一步方向（与 V2.0 升级方案衔接，均为设计未落地）

1. **真实重排序模型联调**：cross_encoder 路径代码已就绪，需在带 GPU/依赖的环境用 bge-reranker 实测精度增益后再决定是否默认开启。
2. **生产向量库切换验证**：chroma 实现已就绪，需在生产配置下回归评测（切换后必须重跑 `eval_rag.py` 对齐 0.942 基准）。
3. **评测集持续扩充**：52 条之后新增语料主题时同步补评测案例，保持「先有尺子再动刀」。
4. **多租户压测**：租户级 BM25 缓存在大租户数下的内存占用需实测（当前设计只保留最新一份，多租户并发场景待验证）。

---

*维护约定：知识库模块每次结构性变更（改检索流程、换向量库、动迁移）应在本文「节点日志」追加一条，附证据路径与实测数字；小改动不必记。*
